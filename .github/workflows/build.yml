name: build

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.0"

      - name: Build pn
        shell: bash
        run: |
          VERSION="build-${GITHUB_RUN_NUMBER}-${GITHUB_SHA::7}"
          mkdir -p dist
          go build -v -ldflags "-s -w -X main.Version=$VERSION" -o dist/pn ./cmd/pn

      - name: Test
        run: go test -v ./...

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: pn-${{ runner.os }}-${{ runner.arch }}
          path: dist/pn${{ runner.os == 'Windows' && '.exe' || '' }}
